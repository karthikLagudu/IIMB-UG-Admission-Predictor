services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: iima_predictor
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - iima_postgres_data:/var/lib/postgresql/data

volumes:
  iima_postgres_data:
